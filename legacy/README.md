# Legacy Archive

Read-only historical material retained for architecture study, migration reference, and regression investigation.

- `v2.2.0/` is the complete 44-file snapshot of the previous AI Elementor Sync system.
- `site-workflows/` contains superseded deployment experiments and pilot content workflows.

Nothing under `legacy/` is part of the active runtime. Do not install its plugins, execute its scripts, or use its manifests against a current website without explicit approval and a dedicated migration review.
